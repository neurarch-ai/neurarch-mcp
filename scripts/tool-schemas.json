{
  "get_model_summary": {
    "type": "object",
    "properties": {
      "model_path": {
        "type": "string",
        "description": "Optional: answer about another model instead (a .py/.neurarch.json path, zoo:<id>, or hf:<org/name>)."
      }
    },
    "additionalProperties": false
  },
  "describe_architecture": {
    "type": "object",
    "properties": {
      "model_path": {
        "type": "string",
        "description": "Optional: answer about another model instead (a .py/.neurarch.json path, zoo:<id>, or hf:<org/name>)."
      }
    },
    "additionalProperties": false
  },
  "get_layer": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Layer name or id. Substring match if no exact hit."
      },
      "model_path": {
        "type": "string",
        "description": "Optional: answer about another model instead (a .py/.neurarch.json path, zoo:<id>, or hf:<org/name>)."
      }
    },
    "required": [
      "name"
    ],
    "additionalProperties": false
  },
  "find_layers": {
    "type": "object",
    "properties": {
      "type": {
        "type": "string",
        "description": "Exact component type (e.g. \"conv2d\", \"linear\", \"multiHeadAttention\")."
      },
      "namePattern": {
        "type": "string",
        "description": "Regex source matched against layer name."
      },
      "scope": {
        "type": "string",
        "description": "Scope prefix, e.g. \"encoder.layer.3\" (matches that scope and anything nested under it)."
      },
      "augmentation": {
        "type": "string",
        "description": "Only layers carrying this augmentation overlay, e.g. \"freeze\", \"quantize_int8\", \"gradient_checkpoint\", \"amp\"."
      },
      "sortByParams": {
        "type": "boolean",
        "description": "When true, rank results by estimated parameter count (largest first) before applying limit."
      },
      "limit": {
        "type": "number",
        "description": "Max results (default 100)."
      },
      "model_path": {
        "type": "string",
        "description": "Optional: answer about another model instead (a .py/.neurarch.json path, zoo:<id>, or hf:<org/name>)."
      }
    },
    "additionalProperties": false
  },
  "compare_layers": {
    "type": "object",
    "properties": {
      "a": {
        "type": "string",
        "description": "First layer name or id."
      },
      "b": {
        "type": "string",
        "description": "Second layer name or id."
      },
      "model_path": {
        "type": "string",
        "description": "Optional: answer about another model instead (a .py/.neurarch.json path, zoo:<id>, or hf:<org/name>)."
      }
    },
    "required": [
      "a",
      "b"
    ],
    "additionalProperties": false
  },
  "layer_impact": {
    "type": "object",
    "properties": {
      "names": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Layer names or ids to use as targets."
      },
      "namePattern": {
        "type": "string",
        "description": "Alternative to names — regex source matching layer names."
      },
      "maxDistance": {
        "type": "number",
        "description": "Limit BFS depth. Omit for full reachability."
      },
      "includeUpstream": {
        "type": "boolean",
        "description": "Default true. Set false to only return downstream."
      },
      "model_path": {
        "type": "string",
        "description": "Optional: answer about another model instead (a .py/.neurarch.json path, zoo:<id>, or hf:<org/name>)."
      }
    },
    "additionalProperties": false
  },
  "param_count_by_block": {
    "type": "object",
    "properties": {
      "groupBy": {
        "type": "string",
        "enum": [
          "block",
          "scope",
          "type"
        ],
        "description": "Default \"block\" (collapsed groups). Falls back to scope when no groups exist."
      },
      "model_path": {
        "type": "string",
        "description": "Optional: answer about another model instead (a .py/.neurarch.json path, zoo:<id>, or hf:<org/name>)."
      }
    },
    "additionalProperties": false
  },
  "flops_by_block": {
    "type": "object",
    "properties": {
      "groupBy": {
        "type": "string",
        "enum": [
          "block",
          "scope",
          "type"
        ]
      },
      "model_path": {
        "type": "string",
        "description": "Optional: answer about another model instead (a .py/.neurarch.json path, zoo:<id>, or hf:<org/name>)."
      }
    },
    "additionalProperties": false
  },
  "mermaid_diagram": {
    "type": "object",
    "properties": {
      "maxLayers": {
        "type": "number",
        "description": "Truncate after N layers (default 60). Returns a note when truncated."
      },
      "model_path": {
        "type": "string",
        "description": "Optional: answer about another model instead (a .py/.neurarch.json path, zoo:<id>, or hf:<org/name>)."
      }
    },
    "additionalProperties": false
  },
  "list_blocks": {
    "type": "object",
    "properties": {
      "model_path": {
        "type": "string",
        "description": "Optional: answer about another model instead (a .py/.neurarch.json path, zoo:<id>, or hf:<org/name>)."
      }
    },
    "additionalProperties": false
  },
  "get_block": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Group name or id, or a scope prefix (e.g. \"encoder\" matches \"encoder.layer.0\")."
      },
      "model_path": {
        "type": "string",
        "description": "Optional: answer about another model instead (a .py/.neurarch.json path, zoo:<id>, or hf:<org/name>)."
      }
    },
    "required": [
      "name"
    ],
    "additionalProperties": false
  },
  "diff_models": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Path to the other .neurarch.json file to compare against."
      },
      "model_path": {
        "type": "string",
        "description": "Optional: answer about another model instead (a .py/.neurarch.json path, zoo:<id>, or hf:<org/name>)."
      }
    },
    "required": [
      "path"
    ],
    "additionalProperties": false
  },
  "validate_model": {
    "type": "object",
    "properties": {
      "model_path": {
        "type": "string",
        "description": "Optional: answer about another model instead (a .py/.neurarch.json path, zoo:<id>, or hf:<org/name>)."
      }
    },
    "additionalProperties": false
  },
  "lint_model": {
    "type": "object",
    "properties": {
      "severity": {
        "type": "string",
        "enum": [
          "block",
          "warn",
          "info"
        ],
        "description": "Optional minimum severity to report. Defaults to reporting everything."
      },
      "model_path": {
        "type": "string",
        "description": "Optional: answer about another model instead (a .py/.neurarch.json path, zoo:<id>, or hf:<org/name>)."
      }
    },
    "additionalProperties": false
  },
  "check_design": {
    "type": "object",
    "properties": {
      "verbose": {
        "type": "boolean",
        "description": "Include every stage's machine-readable data (cost breakdown, deploy targets, contract). Default false: verdict, findings, stage headlines and the decision."
      },
      "ask_user": {
        "type": "boolean",
        "description": "If the verdict ends in a decision only the human can make, ask them through the client (MCP elicitation) and include their answer. Default false."
      },
      "model_path": {
        "type": "string",
        "description": "Optional: answer about another model instead (a .py/.neurarch.json path, zoo:<id>, or hf:<org/name>)."
      }
    },
    "additionalProperties": false
  },
  "find_path": {
    "type": "object",
    "properties": {
      "from": {
        "type": "string",
        "description": "Source layer name or id."
      },
      "to": {
        "type": "string",
        "description": "Target layer name or id."
      },
      "model_path": {
        "type": "string",
        "description": "Optional: answer about another model instead (a .py/.neurarch.json path, zoo:<id>, or hf:<org/name>)."
      }
    },
    "required": [
      "from",
      "to"
    ],
    "additionalProperties": false
  },
  "list_connections": {
    "type": "object",
    "properties": {
      "from": {
        "type": "string",
        "description": "Optional source layer name or id filter."
      },
      "to": {
        "type": "string",
        "description": "Optional target layer name or id filter."
      },
      "limit": {
        "type": "number",
        "description": "Max results (default 200)."
      },
      "model_path": {
        "type": "string",
        "description": "Optional: answer about another model instead (a .py/.neurarch.json path, zoo:<id>, or hf:<org/name>)."
      }
    },
    "additionalProperties": false
  },
  "list_hyperparams": {
    "type": "object",
    "properties": {
      "model_path": {
        "type": "string",
        "description": "Optional: answer about another model instead (a .py/.neurarch.json path, zoo:<id>, or hf:<org/name>)."
      }
    },
    "additionalProperties": false
  },
  "get_design_notes": {
    "type": "object",
    "properties": {
      "layer": {
        "type": "string",
        "description": "Optional layer name or id. When set, return only notes affecting that layer."
      },
      "model_path": {
        "type": "string",
        "description": "Optional: answer about another model instead (a .py/.neurarch.json path, zoo:<id>, or hf:<org/name>)."
      }
    },
    "additionalProperties": false
  },
  "rank_designs": {
    "type": "object",
    "properties": {
      "candidates": {
        "type": "array",
        "minItems": 1,
        "maxItems": 32,
        "items": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string",
              "description": "Label for this candidate in the result. Defaults to the path or candidate-N."
            },
            "model_path": {
              "type": "string",
              "description": "A .py or .neurarch.json path, or zoo:<id> / hf:<org/name>."
            },
            "model": {
              "type": "object",
              "description": "An inline graph ({components, connections}) instead of a path."
            }
          },
          "additionalProperties": false
        },
        "description": "The designs to order. Each needs either model_path or model."
      },
      "include_current": {
        "type": "boolean",
        "description": "Also rank the model this server was started with, as \"current\". Default false."
      },
      "tie_break": {
        "type": "string",
        "enum": [
          "none",
          "cost",
          "params"
        ],
        "description": "Break a measured tie: \"cost\" (cheapest first) or \"params\" (smallest first). Never crosses a measured rank. Default \"none\"."
      },
      "model_path": {
        "type": "string",
        "description": "Optional: answer about another model instead (a .py/.neurarch.json path, zoo:<id>, or hf:<org/name>)."
      }
    },
    "required": [
      "candidates"
    ],
    "additionalProperties": false
  },
  "export_pytorch": {
    "type": "object",
    "properties": {
      "save_to": {
        "type": "string",
        "description": "Optional path to write the generated .py to. Requires --write."
      },
      "model_path": {
        "type": "string",
        "description": "Optional: answer about another model instead (a .py/.neurarch.json path, zoo:<id>, or hf:<org/name>)."
      }
    },
    "additionalProperties": false
  },
  "list_architectures": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "Substring match on id, title, type or domain (case-insensitive)."
      },
      "domain": {
        "type": "string",
        "description": "Exact domain filter, e.g. \"Language models\"."
      },
      "attention": {
        "type": "string",
        "description": "Only entries using this attention type, e.g. \"groupedQueryAttention\"."
      },
      "model_path": {
        "type": "string",
        "description": "Optional: answer about another model instead (a .py/.neurarch.json path, zoo:<id>, or hf:<org/name>)."
      }
    },
    "additionalProperties": false
  },
  "load_architecture": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string",
        "description": "Entry id from list_architectures, e.g. \"qwen2.5-7b\"."
      },
      "save_to": {
        "type": "string",
        "description": "Optional path for a .neurarch.json copy. Requires --write."
      },
      "model_path": {
        "type": "string",
        "description": "Optional: answer about another model instead (a .py/.neurarch.json path, zoo:<id>, or hf:<org/name>)."
      }
    },
    "required": [
      "id"
    ],
    "additionalProperties": false
  },
  "find_models": {
    "type": "object",
    "properties": {
      "dir": {
        "type": "string",
        "description": "Directory to scan. Defaults to the directory of the model this server was started with."
      },
      "max_files": {
        "type": "integer",
        "minimum": 1,
        "maximum": 5000,
        "description": "Cap on files opened (default 400)."
      },
      "model_path": {
        "type": "string",
        "description": "Optional: answer about another model instead (a .py/.neurarch.json path, zoo:<id>, or hf:<org/name>)."
      }
    },
    "additionalProperties": false
  },
  "suggest_fix": {
    "type": "object",
    "properties": {
      "rule": {
        "type": "string",
        "description": "Only this rule id (e.g. head-dim-divisibility). Default: every finding."
      },
      "layer": {
        "type": "string",
        "description": "Only findings on this layer name."
      },
      "severity": {
        "type": "string",
        "enum": [
          "block",
          "warn",
          "info"
        ],
        "description": "Minimum severity to fix. Default: warn."
      },
      "save_to": {
        "type": "string",
        "description": "Write the patched file here (exact fixes only). Requires --write; never the source path itself."
      },
      "model_path": {
        "type": "string",
        "description": "Optional: answer about another model instead (a .py/.neurarch.json path, zoo:<id>, or hf:<org/name>)."
      }
    },
    "additionalProperties": false
  },
  "trace_model": {
    "type": "object",
    "properties": {
      "target": {
        "type": "string",
        "description": "\"path/to/model.py:ClassName\", \"pkg.module:factory\", or \"hf:org/name\"."
      },
      "input": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "minItems": 1,
        "description": "Input dims, batch first, one per model input: [\"1,3,224,224\"] or [\"1,128\"]. Append \":long\" for token ids."
      },
      "dtype": {
        "type": "string",
        "description": "Default dtype for inputs (float32; long for hf: targets)."
      },
      "depth": {
        "type": "integer",
        "minimum": 1,
        "description": "Stop descending at this module depth (default: leaf modules)."
      },
      "name": {
        "type": "string",
        "description": "Graph name (default: the class or repo name)."
      },
      "save_to": {
        "type": "string",
        "description": "Also write the traced .neurarch.json here. Requires --write."
      },
      "timeout_s": {
        "type": "integer",
        "minimum": 5,
        "maximum": 600,
        "description": "Kill the trace after this many seconds (default 120)."
      },
      "model_path": {
        "type": "string",
        "description": "Optional: answer about another model instead (a .py/.neurarch.json path, zoo:<id>, or hf:<org/name>)."
      }
    },
    "required": [
      "target",
      "input"
    ],
    "additionalProperties": false
  }
}
